defaults:
  - model: small
  - train: cuda
  - _self_

seed: 42
wandb_project: krasnal
puzzle_eval:
  enabled: true
  path: data/puzzles_filtered.jsonl
  sample_size: null
  seed: 42
  log_mrr: false
  log_bucket_metrics: false

hydra:
  run:
    dir: .
  output_subdir: null
